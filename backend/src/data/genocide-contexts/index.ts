import { GenocideContext } from "../../features/genocide-context";
import palestinianGenocideContext from "./palestinian";

const genocideContexts : GenocideContext[] = [
    palestinianGenocideContext
]

export default genocideContexts;
